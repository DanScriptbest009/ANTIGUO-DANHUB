local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Camera = Workspace.CurrentCamera
local player = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")

local localTeamCache = nil
local playerList = {}

for _, p in pairs(Players:GetPlayers()) do
    if p ~= player then
        playerList[p] = true
    end
end

Players.PlayerAdded:Connect(function(p)
    if p ~= player then
        playerList[p] = true
    end
end)

Players.PlayerRemoving:Connect(function(p)
    playerList[p] = nil
end)

local function getLocalTeam()
    local playerGui = player:FindFirstChildOfClass("PlayerGui")
    if not playerGui then return nil end
    local main = playerGui:FindFirstChild("Main")
    if not main then return nil end
    local mainGameFrame = main:FindFirstChild("MainGameFrame")
    if not mainGameFrame then return nil end
    local playersFrame = mainGameFrame:FindFirstChild("PlayersFrame")
    if not playersFrame then return nil end
    
    local teamRed = playersFrame:FindFirstChild("TeamRedFrame")
    local teamBlue = playersFrame:FindFirstChild("TeamBlueFrame")
    
    if teamRed and teamRed:FindFirstChild(player.Name) then
        return "Red"
    elseif teamBlue and teamBlue:FindFirstChild(player.Name) then
        return "Blue"
    end
    return nil
end

local function refreshLocalTeam()
    localTeamCache = getLocalTeam()
    return localTeamCache
end

local function isEnemy(targetPlayer)
    if localTeamCache == nil then
        refreshLocalTeam()
    end
    
    if localTeamCache == nil then return true end
    
    local playerGui = player:FindFirstChildOfClass("PlayerGui")
    if not playerGui then return true end
    local main = playerGui:FindFirstChild("Main")
    if not main then return true end
    local mainGameFrame = main:FindFirstChild("MainGameFrame")
    if not mainGameFrame then return true end
    local playersFrame = mainGameFrame:FindFirstChild("PlayersFrame")
    if not playersFrame then return true end
    
    local teamRed = playersFrame:FindFirstChild("TeamRedFrame")
    local teamBlue = playersFrame:FindFirstChild("TeamBlueFrame")
    
    local targetName = targetPlayer.Name
    
    if localTeamCache == "Red" then
        if teamBlue and teamBlue:FindFirstChild(targetName) then
            return true
        end
        return false
    elseif localTeamCache == "Blue" then
        if teamRed and teamRed:FindFirstChild(targetName) then
            return true
        end
        return false
    end
    
    return true
end

task.spawn(function()
    while true do
        refreshLocalTeam()
        task.wait(2)
    end
end)

spawn(function()
    local gui = Instance.new("ScreenGui")
    gui.ResetOnSpawn = false
    gui.Parent = player:WaitForChild("PlayerGui")
    local frame = Instance.new("Frame", gui)
    frame.Size = UDim2.new(0, 490, 0, 100)
    frame.Position = UDim2.new(0.5, -230, -0.2, 0)
    frame.BackgroundColor3 = Color3.fromRGB(10, 10, 25)
    frame.BackgroundTransparency = 0.1
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 24)
    local stroke1 = Instance.new("UIStroke", frame)
    stroke1.Thickness = 6
    stroke1.Color = Color3.fromRGB(255, 50, 100)
    local glow = Instance.new("UIStroke", frame)
    glow.Thickness = 16
    glow.Color = Color3.fromRGB(255, 80, 150)
    glow.Transparency = 0.7
    local txt = Instance.new("TextLabel", frame)
    txt.Size = UDim2.new(1,0,1,0)
    txt.BackgroundTransparency = 1
    txt.Text = " GRACIAS POR USAR DANHUB♥️"
    txt.TextColor3 = Color3.fromRGB(255,120,180)
    txt.Font = Enum.Font.GothamBlack
    txt.TextSize = 36
    txt.TextStrokeTransparency = 0.5
    txt.TextStrokeColor3 = Color3.fromRGB(255,0,100)
    frame:TweenPosition(UDim2.new(0.5,-230,0.35,-50), "Out", "Quint", 0.8, true)
    task.wait(3.5)
    frame:TweenPosition(UDim2.new(0.5,-230,-0.3,0), "In", "Quint", 0.6, true)
    task.wait(0.7)
    gui:Destroy()
end)

local gui = Instance.new("ScreenGui")
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 220, 0, 445)
main.Position = UDim2.new(1, -230, 0, 10)
main.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
main.Active = true
main.Draggable = true
main.Visible = false
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 16)
local stroke = Instance.new("UIStroke", main)
stroke.Thickness = 6
stroke.Color = Color3.fromRGB(255, 80, 80)

local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1, 0, 0, 45)
title.BackgroundTransparency = 1
title.Text = "ONICHAN UWU"
title.TextColor3 = Color3.fromRGB(255, 80, 80)
title.Font = Enum.Font.GothamBlack
title.TextSize = 24

local openBtn = Instance.new("TextButton", gui)
openBtn.Size = UDim2.new(0, 45, 0, 45)
openBtn.Position = UDim2.new(1, -55, 0, 10)
openBtn.BackgroundTransparency = 1
openBtn.Text = ""
openBtn.Active = true
openBtn.Draggable = true
openBtn.Selectable = true

local img = Instance.new("ImageLabel", openBtn)
img.Size = UDim2.new(1,0,1,0)
img.BackgroundTransparency = 1
img.Image = "rbxassetid://103514524013687"
img.ImageTransparency = 0

Instance.new("UICorner", openBtn).CornerRadius = UDim.new(0, 14)

local glow = Instance.new("UIStroke", openBtn)
glow.Thickness = 4
glow.Color = Color3.fromRGB(255, 70, 150)
glow.Transparency = 0.5

local glowOuter = Instance.new("UIStroke", openBtn)
glowOuter.Thickness = 9
glowOuter.Color = Color3.fromRGB(255, 100, 200)
glowOuter.Transparency = 0.85

openBtn.MouseButton1Click:Connect(function()
    main.Visible = not main.Visible
    img.ImageTransparency = 0.4
    task.wait(0.1)
    img.ImageTransparency = 0
end)

openBtn.MouseEnter:Connect(function()
    openBtn:TweenSize(UDim2.new(0, 50, 0, 50), "Out", "Quint", 0.2, true)
end)
openBtn.MouseLeave:Connect(function()
    openBtn:TweenSize(UDim2.new(0, 45, 0, 45), "Out", "Quint", 0.2, true)
end)

local chamsBtn = Instance.new("TextButton", main)
chamsBtn.Size = UDim2.new(1, -16, 0, 35)
chamsBtn.Position = UDim2.new(0, 8, 0, 55)
chamsBtn.Text = "CHAMS ESP: OFF"
chamsBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
chamsBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
chamsBtn.Font = Enum.Font.GothamBold
chamsBtn.TextSize = 13
Instance.new("UICorner", chamsBtn).CornerRadius = UDim.new(0, 12)

local torsoShotBtn = Instance.new("TextButton", main)
torsoShotBtn.Size = UDim2.new(1, -16, 0, 35)
torsoShotBtn.Position = UDim2.new(0, 8, 0, 98)
torsoShotBtn.Text = "PERFECT TORSO SHOT: OFF"
torsoShotBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
torsoShotBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
torsoShotBtn.Font = Enum.Font.GothamBold
torsoShotBtn.TextSize = 13
Instance.new("UICorner", torsoShotBtn).CornerRadius = UDim.new(0, 12)

local instaKillBtn = Instance.new("TextButton", main)
instaKillBtn.Size = UDim2.new(1, -16, 0, 35)
instaKillBtn.Position = UDim2.new(0, 8, 0, 141)
instaKillBtn.Text = "INSTA KILL: OFF"
instaKillBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
instaKillBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
instaKillBtn.Font = Enum.Font.GothamBold
instaKillBtn.TextSize = 13
Instance.new("UICorner", instaKillBtn).CornerRadius = UDim.new(0, 12)

local autoTorsoBtn = Instance.new("TextButton", main)
autoTorsoBtn.Size = UDim2.new(1, -16, 0, 35)
autoTorsoBtn.Position = UDim2.new(0, 8, 0, 184)
autoTorsoBtn.Text = "AUTO SHOOT: OFF"
autoTorsoBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
autoTorsoBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
autoTorsoBtn.Font = Enum.Font.GothamBold
autoTorsoBtn.TextSize = 13
Instance.new("UICorner", autoTorsoBtn).CornerRadius = UDim.new(0, 12)

local autoShootV1Btn = Instance.new("TextButton", main)
autoShootV1Btn.Size = UDim2.new(1, -16, 0, 35)
autoShootV1Btn.Position = UDim2.new(0, 8, 0, 227)
autoShootV1Btn.Text = "AUTO SHOOT V1: OFF"
autoShootV1Btn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
autoShootV1Btn.TextColor3 = Color3.fromRGB(255, 100, 100)
autoShootV1Btn.Font = Enum.Font.GothamBold
autoShootV1Btn.TextSize = 13
Instance.new("UICorner", autoShootV1Btn).CornerRadius = UDim.new(0, 12)

local tik = Instance.new("TextButton", main)
tik.Size = UDim2.new(1, -16, 0, 35)
tik.Position = UDim2.new(0, 8, 0, 270)
tik.Text = "TikTok: dan8198_1"
tik.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
tik.TextColor3 = Color3.fromRGB(255, 100, 255)
tik.Font = Enum.Font.GothamBold
tik.TextSize = 12
Instance.new("UICorner", tik).CornerRadius = UDim.new(0, 10)

local discordBtn = Instance.new("TextButton", main)
discordBtn.Size = UDim2.new(1, -16, 0, 35)
discordBtn.Position = UDim2.new(0, 8, 0, 313)
discordBtn.Text = "Discord"
discordBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
discordBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
discordBtn.Font = Enum.Font.GothamBold
discordBtn.TextSize = 13
Instance.new("UICorner", discordBtn).CornerRadius = UDim.new(0, 10)

local function getCurrentGun()
    local char = player.Character
    if not char then return nil end
    for _, tool in ipairs(char:GetChildren()) do
        if tool:IsA("Tool") and tool:FindFirstChild("Handle") then
            return tool
        end
    end
    return nil
end

local function isEnemyInFront(enemyChar)
    local myChar = player.Character
    if not myChar or not myChar:FindFirstChild("HumanoidRootPart") or not myChar:FindFirstChild("Head") then return false end
    if not enemyChar or not enemyChar:FindFirstChild("HumanoidRootPart") then return false end
    local myRoot = myChar.HumanoidRootPart
    local enemyRoot = enemyChar.HumanoidRootPart
    local directionToEnemy = (enemyRoot.Position - myRoot.Position).Unit
    local lookDirection = Camera.CFrame.LookVector
    local dot = directionToEnemy:Dot(lookDirection)
    return dot > 0.1
end

local torsoShotEnabled = false
torsoShotBtn.MouseButton1Click:Connect(function()
    torsoShotEnabled = not torsoShotEnabled
    torsoShotBtn.Text = "PERFECT TORSO SHOT: " .. (torsoShotEnabled and "ON" or "OFF")
    torsoShotBtn.TextColor3 = torsoShotEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 100, 100)
    torsoShotBtn.BackgroundColor3 = torsoShotEnabled and Color3.fromRGB(0, 60, 0) or Color3.fromRGB(30, 30, 40)
end)

local function getClosestEnemyInFront()
    local char = player.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return nil end
    local myRoot = char.HumanoidRootPart
    local origin = char:FindFirstChild("Head") and char.Head.Position or myRoot.Position
    local closest = nil
    local bestDist = math.huge
    for _, p in Players:GetPlayers() do
        if p ~= player and p.Character and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 and isEnemy(p) then
            local enemyChar = p.Character
            local torso = enemyChar:FindFirstChild("HumanoidRootPart") or enemyChar:FindFirstChild("Torso") or enemyChar:FindFirstChild("UpperTorso")
            if torso and isEnemyInFront(enemyChar) then
                local dist = (torso.Position - myRoot.Position).Magnitude
                if dist < bestDist then
                    local dir = torso.Position - origin
                    local params = RaycastParams.new()
                    params.FilterDescendantsInstances = {char}
                    params.FilterType = Enum.RaycastFilterType.Exclude
                    local result = Workspace:Raycast(origin, dir * 1000, params)
                    if not result or result.Instance:IsDescendantOf(enemyChar) then
                        bestDist = dist
                        closest = p
                    end
                end
            end
        end
    end
    return closest
end

local function getClosestEnemyAnywhere()
    local char = player.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return nil end
    local myRoot = char.HumanoidRootPart
    local closest = nil
    local bestDist = math.huge
    for _, p in Players:GetPlayers() do
        if p ~= player and p.Character and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 and isEnemy(p) then
            local enemyChar = p.Character
            local torso = enemyChar:FindFirstChild("HumanoidRootPart") or enemyChar:FindFirstChild("Torso") or enemyChar:FindFirstChild("UpperTorso")
            if torso then
                local dist = (torso.Position - myRoot.Position).Magnitude
                if dist < bestDist then
                    bestDist = dist
                    closest = p
                end
            end
        end
    end
    return closest
end

local function perfectTorsoShot()
    if not torsoShotEnabled then return end
    local gun = getCurrentGun()
    if not gun then return end
    local target = getClosestEnemyInFront()
    if not target or not target.Character then return end
    local torso = target.Character:FindFirstChild("HumanoidRootPart") or target.Character:FindFirstChild("Torso") or target.Character:FindFirstChild("UpperTorso")
    if not torso then return end
    local handle = gun:FindFirstChild("Handle")
    if not handle then return end
    local barrel = gun:FindFirstChild("Barrel") or handle:FindFirstChild("Barrel") or handle:FindFirstChild("Muzzle") or handle:FindFirstChildWhichIsA("Attachment") or handle
    local muzzlePos = barrel.Position
    local targetPos = torso.Position
    local fire = gun:FindFirstChild("fire")
    if fire then pcall(function() fire:FireServer() end) end
    local beam = gun:FindFirstChild("showBeam")
    if beam then pcall(function() beam:FireServer(targetPos, muzzlePos, handle) end) end
    local kill = gun:FindFirstChild("kill")
    if kill then pcall(function() kill:FireServer(target) end) end
end

local function connectGun(tool)
    if tool:IsA("Tool") then
        tool.Activated:Connect(function()
            if torsoShotEnabled then
                task.spawn(perfectTorsoShot)
            end
        end)
    end
end

player.CharacterAdded:Connect(function(char)
    char.ChildAdded:Connect(connectGun)
    for _, tool in char:GetChildren() do
        connectGun(tool)
    end
end)
if player.Character then
    for _, tool in player.Character:GetChildren() do
        connectGun(tool)
    end
end

local instaKillEnabled = false
local KnifeKill = ReplicatedStorage:FindFirstChild("Packages") and ReplicatedStorage.Packages:FindFirstChild("Networking") and ReplicatedStorage.Packages.Networking:FindFirstChild("RE/Combat/KnifeKill")

instaKillBtn.MouseButton1Click:Connect(function()
    instaKillEnabled = not instaKillEnabled
    instaKillBtn.Text = "INSTA KILL: " .. (instaKillEnabled and "ON" or "OFF")
    instaKillBtn.TextColor3 = instaKillEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 100, 100)
    instaKillBtn.BackgroundColor3 = instaKillEnabled and Color3.fromRGB(0, 60, 0) or Color3.fromRGB(30, 30, 40)
end)

local hasTool = false
local function UpdateToolState()
    hasTool = false
    if player.Backpack then
        for _, v in ipairs(player.Backpack:GetChildren()) do
            if v.ClassName == "Tool" then
                hasTool = true
                break
            end
        end
    end
    if player.Character then
        for _, v in ipairs(player.Character:GetChildren()) do
            if v.ClassName == "Tool" then
                hasTool = true
                break
            end
        end
    end
end

if player.Backpack then
    player.Backpack.ChildAdded:Connect(UpdateToolState)
    player.Backpack.ChildRemoved:Connect(UpdateToolState)
end
player.CharacterAdded:Connect(function(char)
    char.ChildAdded:Connect(function(child)
        if child.ClassName == "Tool" then UpdateToolState() end
    end)
    char.ChildRemoved:Connect(function(child)
        if child.ClassName == "Tool" then UpdateToolState() end
    end)
    UpdateToolState()
end)
UpdateToolState()

task.spawn(function()
    while true do
        if instaKillEnabled and hasTool and KnifeKill then
            for p in pairs(playerList) do
                if p ~= player then
                    pcall(function()
                        KnifeKill:FireServer(p)
                    end)
                end
            end
        end
        task.wait(0.0001)
    end
end)

local highlightObjects = {}
local chamsOn = false

local function updateESP()
    if not chamsOn then return end
    
    refreshLocalTeam()
    
    for _, otherPlayer in pairs(Players:GetPlayers()) do
        if otherPlayer ~= player then
            local character = otherPlayer.Character
            if character and character:FindFirstChild("HumanoidRootPart") then
                local humanoid = character:FindFirstChild("Humanoid")
                if humanoid and humanoid.Health > 0 then
                    local highlight = highlightObjects[otherPlayer]
                    
                    if not highlight or highlight.Parent ~= character then
                        if highlight then
                            pcall(function() highlight:Destroy() end)
                        end
                        
                        highlight = Instance.new("Highlight")
                        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                        highlight.FillTransparency = 0.4
                        highlight.OutlineTransparency = 0
                        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                        
                        if isEnemy(otherPlayer) then
                            highlight.FillColor = Color3.fromRGB(255, 0, 0)
                        else
                            highlight.FillColor = Color3.fromRGB(0, 255, 0)
                        end
                        
                        highlight.Parent = character
                        highlightObjects[otherPlayer] = highlight
                    end
                else
                    if highlightObjects[otherPlayer] then
                        pcall(function() highlightObjects[otherPlayer]:Destroy() end)
                        highlightObjects[otherPlayer] = nil
                    end
                end
            end
        end
    end
end

local function clearESP()
    for _, highlight in pairs(highlightObjects) do
        pcall(function() highlight:Destroy() end)
    end
    highlightObjects = {}
end

chamsBtn.MouseButton1Click:Connect(function()
    chamsOn = not chamsOn
    chamsBtn.Text = "CHAMS ESP: " .. (chamsOn and "ON" or "OFF")
    chamsBtn.TextColor3 = chamsOn and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 100, 100)
    chamsBtn.BackgroundColor3 = chamsOn and Color3.fromRGB(0, 60, 0) or Color3.fromRGB(30, 30, 40)
    if chamsOn then
        refreshLocalTeam()
        updateESP()
    else
        clearESP()
    end
end)

Players.PlayerAdded:Connect(function(p)
    if chamsOn then
        task.wait(0.5)
        refreshLocalTeam()
        updateESP()
    end
end)

Players.PlayerRemoving:Connect(function(p)
    local highlight = highlightObjects[p]
    if highlight then
        pcall(function() highlight:Destroy() end)
        highlightObjects[p] = nil
    end
end)

player.CharacterAdded:Connect(function()
    if chamsOn then
        task.wait(0.5)
        refreshLocalTeam()
        updateESP()
    end
end)

local lastESPUpdate = 0
RunService.Heartbeat:Connect(function()
    if chamsOn then
        local now = tick()
        if now - lastESPUpdate >= 1 then
            pcall(updateESP)
            lastESPUpdate = now
        end
    end
end)

local autoTorsoEnabled = false
local lastTarget = nil
local shootTime = 0

autoTorsoBtn.MouseButton1Click:Connect(function()
    autoTorsoEnabled = not autoTorsoEnabled
    autoTorsoBtn.Text = "AUTO SHOOT: " .. (autoTorsoEnabled and "ON" or "OFF")
    autoTorsoBtn.TextColor3 = autoTorsoEnabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 100, 100)
    autoTorsoBtn.BackgroundColor3 = autoTorsoEnabled and Color3.fromRGB(0, 60, 0) or Color3.fromRGB(30, 30, 40)
    if not autoTorsoEnabled then
        lastTarget = nil
    end
end)

RunService.Heartbeat:Connect(function()
    if not autoTorsoEnabled then 
        return 
    end

    local gun = getCurrentGun()
    if not gun then return end

    local target = getClosestEnemyInFront()

    if target and target.Character then
        if not isEnemy(target) then
            return
        end
        
        if lastTarget ~= target then
            lastTarget = target
            shootTime = tick() + 0.08
        end

        if tick() >= shootTime then
            local torso = target.Character:FindFirstChild("HumanoidRootPart") or 
                         target.Character:FindFirstChild("UpperTorso") or 
                         target.Character:FindFirstChild("Torso")
            if torso then
                local handle = gun:FindFirstChild("Handle")
                if handle then
                    local barrel = gun:FindFirstChild("Barrel") or handle:FindFirstChild("Barrel") or 
                                  handle:FindFirstChild("Muzzle") or handle:FindFirstChildWhichIsA("Attachment") or handle

                    local fire = gun:FindFirstChild("fire")
                    if fire then pcall(function() fire:FireServer() end) end
                    local beam = gun:FindFirstChild("showBeam")
                    if beam then pcall(function() beam:FireServer(torso.Position, barrel.Position, handle) end) end
                    local kill = gun:FindFirstChild("kill")
                    if kill then pcall(function() kill:FireServer(target) end) end

                    task.wait(0.3)
                end
            end
        end
    else
        lastTarget = nil
    end
end)

local autoShootV1Enabled = false
local lastTargetV1 = nil
local shootTimeV1 = 0

autoShootV1Btn.MouseButton1Click:Connect(function()
    autoShootV1Enabled = not autoShootV1Enabled
    autoShootV1Btn.Text = "AUTO SHOOT V1: " .. (autoShootV1Enabled and "ON" or "OFF")
    autoShootV1Btn.TextColor3 = autoShootV1Enabled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 100, 100)
    autoShootV1Btn.BackgroundColor3 = autoShootV1Enabled and Color3.fromRGB(0, 60, 0) or Color3.fromRGB(30, 30, 40)
    if not autoShootV1Enabled then
        lastTargetV1 = nil
    end
end)

RunService.Heartbeat:Connect(function()
    if not autoShootV1Enabled then 
        return 
    end

    local gun = getCurrentGun()
    if not gun then return end

    local target = getClosestEnemyAnywhere()

    if target and target.Character then
        if not isEnemy(target) then
            return
        end
        
        if lastTargetV1 ~= target then
            lastTargetV1 = target
            shootTimeV1 = tick() + 0.08
        end

        if tick() >= shootTimeV1 then
            local torso = target.Character:FindFirstChild("HumanoidRootPart") or 
                         target.Character:FindFirstChild("UpperTorso") or 
                         target.Character:FindFirstChild("Torso")
            if torso then
                local handle = gun:FindFirstChild("Handle")
                if handle then
                    local barrel = gun:FindFirstChild("Barrel") or handle:FindFirstChild("Barrel") or 
                                  handle:FindFirstChild("Muzzle") or handle:FindFirstChildWhichIsA("Attachment") or handle

                    local fire = gun:FindFirstChild("fire")
                    if fire then pcall(function() fire:FireServer() end) end
                    local beam = gun:FindFirstChild("showBeam")
                    if beam then pcall(function() beam:FireServer(torso.Position, barrel.Position, handle) end) end
                    local kill = gun:FindFirstChild("kill")
                    if kill then pcall(function() kill:FireServer(target) end) end

                    task.wait(0.3)
                end
            end
        end
    else
        lastTargetV1 = nil
    end
end)

local function keepESPAlive()
    while true do
        if chamsOn then
            updateESP()
        end
        task.wait(3)
    end
end
task.spawn(keepESPAlive)

local function keepAutoShootTargetValid()
    while true do
        if autoTorsoEnabled and lastTarget then
            if not isEnemy(lastTarget) or not lastTarget.Character or not lastTarget.Character:FindFirstChild("Humanoid") or lastTarget.Character.Humanoid.Health <= 0 then
                lastTarget = nil
            end
        end
        if autoShootV1Enabled and lastTargetV1 then
            if not isEnemy(lastTargetV1) or not lastTargetV1.Character or not lastTargetV1.Character:FindFirstChild("Humanoid") or lastTargetV1.Character.Humanoid.Health <= 0 then
                lastTargetV1 = nil
            end
        end
        task.wait(0.5)
    end
end
task.spawn(keepAutoShootTargetValid)

tik.MouseButton1Click:Connect(function()
    setclipboard("dan8198_1")
    tik.Text = "¡COPIADO!"
    task.wait(1.5)
    tik.Text = "TikTok: dan8198_1"
end)

discordBtn.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/STDQEcD3JT")
    discordBtn.Text = "¡LINK COPIADO!"
    task.wait(1.8)
    discordBtn.Text = "Discord"
end)
